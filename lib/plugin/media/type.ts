export enum Type {
  Image = 0,
  Video = 1,
  Audio = 2,
  PolygonStreaming = 3,
}

export enum ProjectionType {
  Flat = 0,
  Sphere = 1,
}

export enum SourceType {
  Asset = 0,
  Url = 1,
}

export enum PriorityType {
  Default = 1,
  Higher = 2,
  Highest = 3,
  Custom = 4,
}