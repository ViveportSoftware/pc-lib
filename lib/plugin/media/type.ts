export enum Type {
  Image = 0,
  Video = 1,
  Audio = 2,
  PolygonStreaming = 3,
}

export enum ProjectionType {
  Flat = 0,
  Sphere = 1,
}

export enum SourceType {
  Asset = 0,
  Url = 1,
}

export enum PriorityType {
  Low = 0,
  Medium = 1,
  High = 2,
}