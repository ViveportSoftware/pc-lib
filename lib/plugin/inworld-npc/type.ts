export enum Type {
  Base = 0,
}

export enum VrmSourceType {
  Asset = 1,
  Url = 2,
}

export enum AutoFollowMode {
  None = 1,
  PathFinding = 2,
  PhaseWall = 3,
}

export enum GreetingMode {
  None = 1,
  Instruction = 2,
  Verbatim = 3,
}
