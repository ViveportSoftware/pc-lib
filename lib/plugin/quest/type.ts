export enum Type {
  Base = 0,
}

export enum TaskType {
  check = 1,
  progressBar = 2,
  eventListening = 3,
}

export enum ResponseType {
  startQuest = 1,
  resetQuest = 2,
  addTaskProgress = 3,
  completeTask = 4,
}
